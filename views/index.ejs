<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Karate Ranking Dashboard</title>

  <!-- Bootstrap -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

  <!-- Google Font -->
  <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700&display=swap" rel="stylesheet">

  <style>
    body {
      font-family: 'Roboto', sans-serif;
      background: linear-gradient(135deg, #e9f0ff 0%, #fdfdfd 100%);
      padding: 40px 20px;
      color: #333;
    }

    h1 {
      font-weight: 700;
      color: #1d3557;
      margin-bottom: 30px;
      text-align: center;
    }

    .dashboard-links, .year-cards {
      display: flex;
      flex-wrap: wrap;
      gap: 15px;
      justify-content: center;
      margin-bottom: 40px;
    }

    .dashboard-links a {
      background-color: #1d3557;
      color: #fff;
      padding: 12px 24px;
      border-radius: 10px;
      text-decoration: none;
      font-weight: 500;
      transition: background 0.2s;
    }

    .dashboard-links a:hover {
      background-color: #457b9d;
    }

    .year-card {
      background: #fff;
      border-radius: 12px;
      padding: 20px;
      width: 150px;
      text-align: center;
      box-shadow: 0 4px 16px rgba(0,0,0,0.08);
      transition: transform 0.2s, box-shadow 0.2s;
    }

    .year-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 6px 20px rgba(0,0,0,0.12);
    }

    .year-card a {
      display: inline-block;
      margin-top: 10px;
      background-color: #1d3557;
      color: #fff;
      padding: 8px 12px;
      border-radius: 8px;
      text-decoration: none;
      font-weight: 500;
      transition: background 0.2s;
    }

    .year-card a:hover {
      background-color: #457b9d;
    }
  </style>
</head>
<body>
  <h1>Karate Ranking Dashboard</h1>

  <!-- Main Dashboard Links -->
  <div class="dashboard-links">
    <a href="/athletes">Athletes</a>
    <a href="/tournaments">Tournaments</a>
    <a href="/clubs">Clubs</a>
    <a href="/ageCategories">Age Categories</a>
    <a href="/weightCategories">Weight Categories</a>
    <a href="/ranking">Ranking</a>
    <% if(userRole === 'admin' || userRole === 'superuser') { %>
      <a href="/logs">Logs</a>
        <% } %>
   <% if(userRole === 'admin') { %>
      <a href="/logs">Logs</a>
      <a href="/backup">Backup</a>
      <a href="/restore">Restore</a>
    <% } %>

    <a href="/auth/logout">Logout</a>
  </div>

  <!-- Year Preview Cards -->
  <h2 class="text-center mb-4">📅 Προεπισκόπηση Κατάταξης ανά Έτος</h2>
  <div class="year-cards">
    <% 
      const startYear = 2024;
      const currentYear = new Date().getFullYear();
      for (let y = startYear; y <= currentYear; y++) { 
    %>
      <div class="year-card">
        <strong>Έτος <%= y %></strong>
        <a href="/ranking/preview/<%= y %>">Προεπισκόπηση</a>
      </div>
    <% } %>
  </div>
</body>
</html>
