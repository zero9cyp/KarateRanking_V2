<!DOCTYPE html>
<html>
<head>
  <title>Ranking <%= season %></title>
  <link rel="stylesheet" href="/css/bootstrap.min.css">
</head>
<body class="p-4">
  <h2>🏆 Official Ranking – Season <%= season %></h2>
  <table class="table table-striped mt-3">
    <thead>
      <tr>
        <th>#</th><th>Athlete</th><th>Gender</th>
        <th>Age</th><th>Weight</th><th>Club</th><th>Points</th>
      </tr>
    </thead>
    <tbody>
      <% rows.forEach((r, i)=>{ %>
        <tr>
          <td><%= i+1 %></td>
          <td><a href="/athlete/<%= r.id %>/history-v2"><%= r.full_name %></a></td>
          <td><%= r.gender %></td>
          <td><%= r.age_category %></td>
          <td><%= r.weight_category %></td>
          <td><%= r.club %></td>
          <td><%= r.total_points.toFixed(2) %></td>
        </tr>
      <% }) %>
    </tbody>
  </table>
</body>
</html>
