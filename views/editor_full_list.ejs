<div class="container mt-4">
  <h3>🧍‍♂️ Athlete Full Editor</h3>

  <form class="row g-2 my-3" method="get" action="/editor-full">
    <div class="col-md-6">
      <input type="text" class="form-control" name="q" value="<%= q || '' %>" placeholder="Αναζήτηση αθλητή...">
    </div>
    <div class="col-md-2">
      <button class="btn btn-primary w-100">🔎 Αναζήτηση</button>
    </div>
    <div class="col-md-2">
      <a class="btn btn-secondary w-100" href="/editor-full">Καθαρισμός</a>
    </div>
  </form>

  <table class="table table-striped table-bordered align-middle">
    <thead class="table-dark text-center">
      <tr>
        <th>#</th>
        <th>Full Name</th>
        <th>Gender</th>
        <th>Age</th>
        <th>Weight</th>
        <th>Club</th>
        <th>Total</th>
        <th>Status</th>
        <th>Last Review</th>
        <th>Manage</th>
      </tr>
    </thead>
    <tbody>
      <% athletes.forEach((a, i) => { %>
        <tr class="text-center">
          <td><%= i+1 %></td>
          <td class="text-start"><strong><%= a.full_name %></strong></td>
          <td><%= a.gender || '-' %></td>
          <td><%= a.age_category || '-' %></td>
          <td><%= a.weight_category || '-' %></td>
          <td><%= a.club || '-' %></td>
          <td class="fw-bold text-success"><%= a.total_points || 0 %></td>
          <td>
            <% if (a.review_status === 'ok') { %>
              <span class="badge bg-success">OK</span>
            <% } else if (a.review_status === 'issue') { %>
              <span class="badge bg-danger">Issue</span>
            <% } else { %>
              <span class="badge bg-warning text-dark">Pending</span>
            <% } %>
          </td>
          <td><%= a.last_reviewed_at || '—' %></td>
          <td><a class="btn btn-sm btn-outline-primary" href="/editor-full/<%= a.id %>">✏️ Διαχείριση</a></td>
        </tr>
      <% }) %>
    </tbody>
  </table>
</div>
